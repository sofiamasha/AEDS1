#include <stdio.h>
int main (){
float raio, altura,pi, area;
printf ("Digite o valor do raio e da altura separados por espaço: ");
scanf ("%f %f", &altura, &raio);
pi=3.14
area= (pi*(raio*raio));
printf("A area do circulo: %.2f\n", area);
return 0;
}
